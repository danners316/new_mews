<%= form_for [@topic, @post], :remote => true do |f| %>
    <%= f.text_area :body, :rows => 10, :cols => 40 %>
    <%= f.hidden_field :name, :value => "#{current_user.name}" %>
    <%= f.hidden_field :topic_id, :value => "#{@topic.id}" %><br />
    <%= submit_tag "Create" %>
<% end %>